version: '2'
services:
  web:
    image: nginx:1.15
    ports:
      - 8080:80
    volumes:
      - ./web/default.conf:/etc/nginx/conf.d/default.conf
  app:
    build: app
    volumes:
      - ./app/app:/var/www
  db:
    image: mysql:5.7
    volumes:
      - ./db:/var/lib/mysql