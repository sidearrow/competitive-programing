#include <cstdio>
#include <iostream>

using namespace std;

int main() {
  int a, b[1000];
  while (scanf("%d", &a) != EOF) {
    int b = floor(a / 2);
    for (int i = 2; i <= b; i++) {
      if ()
    }
  }
}