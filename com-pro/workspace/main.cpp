#include <stdio.h>
#include <iostream>
#include <string>
#include <vector>
#include <utility>

using namespace std;

int match[9] = {2, 5, 5, 4, 5, 6, 3, 7, 6};

int main() {
    int N, M;
    scanf("%d%d", &N, &M);
    int A[M];
    for (int i = 0; i < M; i++) {
        scanf("%d", A[i]);
    }

}
